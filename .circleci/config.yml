version: 2.1

jobs:
  build:
    docker:
      image: linux-cuda-11:default
      resource_class: small
    steps:
      - run: nvidia-smi
      - run: echo 'Working'

workflows:
  my-workflow:
    jobs:
      - build

